# Project Repository

This is the initial README file for the project.

Build note:
- If you see during build:
  "Browserslist: browsers data (caniuse-lite) is 7 months old..."
  This is informational and does not break CI. You can optionally update locally:
    npm run --prefix taskmaster_frontend browserslist:update